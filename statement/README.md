# Statement package


The package renders the customer-facing period statement as a semantic node
tree plus HTML. See `docs/STATEMENT.md` for the node contract used by support
and accessibility tooling.
